# 다이어트 쇼핑몰 설계 문서 (DESIGN.md)

## 1. 개요

고객의 신체 정보(이름, 성별, 키, 몸무게)와 목표(다이어트 기간, 목표 몸무게)를 입력받아
**개인 맞춤형 다이어트 플랜**을 제시하고, 그 플랜에 맞는 **식단 상품 / 보조식품(제약회사 제품)**을
판매하는 커머스 서비스. 목표 달성 시 기프트를 지급하고, 진행 과정을 기록/비교할 수 있다.

핵심 차별점: 입력값 기반으로 "현재 이미지"와 "목표 개월 수 이후 예상 이미지"를 나란히 보여주는
비주얼 시뮬레이터, 그리고 운동/식단/보조식품 3축 가이드를 기간별로 제공하는 것.

---

## 2. 핵심 사용자 플로우

```
1) 회원가입/로그인
        ↓
2) 프로필 입력: 이름, 성별, 키, 몸무게
        ↓
3) 목표 설정: 목표 몸무게, 다이어트 기간(개월)
        ↓
4) 예상 이미지 모형 생성
   - 현재 체형 이미지 (BMI 기반 실루엣)
   - N개월 후 예상 체형 이미지 (목표 체중 기반 실루엣)
        ↓
5) 플랜 추천: 운동 / 식단 / 보조식품 가이드 (기간별 단계 구성)
        ↓
6) 상품 선택 및 커스텀 구매 (식단 상품 + 보조식품)
        ↓
7) [저장] 버튼 → 스냅샷 기록 (프로필+목표+이미지+플랜을 시점별로 저장)
        ↓
8) 진행 중 재방문 시 실제 체중 업데이트 → 이미지/플랜 자동 재계산 → 이전 기록과 비교
        ↓
9) 목표 달성 판정 → 축하 기프트 자동 지급
        ↓
10) 구매 이력 / 다이어트 기록 히스토리 열람
```

---

## 3. 정보 구조 (IA)

- **홈** — 추천 플랜 배너, 베스트 상품, 성공 후기
- **내 다이어트 플랜**
  - 프로필/목표 설정
  - 예상 이미지 시뮬레이터 (현재 vs 목표)
  - 3축 가이드 (운동 / 식단 / 보조식품) — 기간별 타임라인
  - 기록 히스토리 (스냅샷 비교, 그래프)
- **쇼핑**
  - 식단 상품관
  - 보조식품(제약사 제품)관
  - 플랜 기반 추천 상품 (커스텀 큐레이션)
  - 장바구니 / 주문
- **마이페이지**
  - 프로필
  - 구매 이력
  - 획득 기프트함
  - 진행 기록(타임라인)
- **관리자(백오피스)**
  - 상품/재고 관리
  - 플랜 템플릿(운동/식단/보조식품) 관리
  - 기프트 규칙 관리
  - 주문/회원 관리

---

## 4. 데이터 모델

### 4.1 User (회원)
| 필드 | 타입 | 설명 |
|---|---|---|
| id | UUID | PK |
| name | string | 이름 |
| email | string | 로그인 계정 |
| gender | enum(M/F/기타) | 성별 |
| created_at | datetime | 가입일 |

### 4.2 BodyProfile (신체 정보, 이력 관리)
| 필드 | 타입 | 설명 |
|---|---|---|
| id | UUID | PK |
| user_id | UUID | FK → User |
| height_cm | float | 키 |
| weight_kg | float | 측정 당시 몸무게 |
| bmi | float | 계산값 (weight/height²) |
| measured_at | datetime | 측정 시점 |

> 몸무게는 계속 업데이트되므로 이력 테이블로 관리 (진행 그래프에 사용).

### 4.3 DietGoal (다이어트 목표)
| 필드 | 타입 | 설명 |
|---|---|---|
| id | UUID | PK |
| user_id | UUID | FK |
| start_weight_kg | float | 시작 체중 |
| target_weight_kg | float | 목표 체중 |
| duration_months | int | 목표 기간(개월) |
| start_date | date | 시작일 |
| target_date | date | 목표일 (start_date + duration) |
| status | enum | in_progress / achieved / abandoned |

### 4.4 BodySimulation (예상 이미지 모형)
| 필드 | 타입 | 설명 |
|---|---|---|
| id | UUID | PK |
| goal_id | UUID | FK → DietGoal |
| current_image_params | JSON | 현재 실루엣 렌더 파라미터 (체형 카테고리, BMI 등) |
| target_image_params | JSON | 목표 실루엣 렌더 파라미터 |
| generated_at | datetime | 생성 시점 |

> 실제 사진 합성이 아니라, **BMI/체형 카테고리 기반의 파라메트릭 실루엣(일러스트/3D 아바타)** 방식을
> 1차 구현으로 권장. (사진 기반 AI 합성은 법적/윤리 리스크와 비용이 높아 2차 확장 항목으로 분리)

### 4.5 PlanSnapshot (저장 버튼 클릭 시 기록)
| 필드 | 타입 | 설명 |
|---|---|---|
| id | UUID | PK |
| user_id | UUID | FK |
| goal_id | UUID | FK |
| body_profile_id | UUID | FK (당시 신체정보) |
| simulation_id | UUID | FK (당시 이미지 모형) |
| exercise_plan | JSON | 당시 운동 가이드 |
| diet_plan | JSON | 당시 식단 가이드 |
| supplement_plan | JSON | 당시 보조식품 가이드 |
| selected_products | JSON | 당시 선택 상품 목록 |
| saved_at | datetime | 저장 시점 |
| note | string | 사용자 메모(선택) |

> "수정하면 업데이트, 저장을 누르면 기록"의 구현 방식:
> - 입력값(키/몸무게/목표) 수정 → **DietGoal/BodyProfile을 업데이트**하고 현재 화면(이미지·플랜)은 실시간 재계산.
> - **[저장]** 버튼 → 그 시점의 상태를 통째로 `PlanSnapshot`으로 별도 저장 (불변 기록).
> - 기록 히스토리 화면에서 여러 `PlanSnapshot`을 시간순으로 비교(그래프/이미지 슬라이더).

### 4.6 Product (상품)
| 필드 | 타입 | 설명 |
|---|---|---|
| id | UUID | PK |
| category | enum | diet_meal(식단) / supplement(보조식품) |
| manufacturer | string | 제약사/제조사명 (보조식품용) |
| name, description, price, stock, image_url | ... | 기본 상품 정보 |
| suitable_phase | enum | early/mid/late (플랜 단계별 추천 태그) |

### 4.7 PlanTemplate (운동/식단/보조식품 가이드 템플릿)
| 필드 | 타입 | 설명 |
|---|---|---|
| id | UUID | PK |
| goal_range | JSON | 적용 조건 (감량폭, 기간, 성별 등) |
| phase | enum | 1단계/2단계/3단계 (기간을 N구간으로 분할) |
| exercise_guide | text/JSON | 운동 방법 가이드 |
| diet_guide | text/JSON | 식단 가이드 + 추천 상품군 |
| supplement_guide | text/JSON | 보조식품 가이드 + 추천 상품군 |

### 4.8 Order / OrderItem (구매 이력)
| 필드 | 타입 | 설명 |
|---|---|---|
| id | UUID | PK |
| user_id | UUID | FK |
| goal_id | UUID | FK (어떤 플랜 진행 중 구매인지 연결) |
| items | [OrderItem] | 상품, 수량, 가격 |
| total_price | number | 합계 |
| ordered_at | datetime | 주문일 |
| status | enum | 결제완료/배송중/완료/취소 |

### 4.9 Gift (성공 기프트)
| 필드 | 타입 | 설명 |
|---|---|---|
| id | UUID | PK |
| goal_id | UUID | FK |
| rule | string | 지급 기준 (예: 목표체중 도달 & 기간 내 달성) |
| gift_type | enum | 쿠폰/사은품/포인트 |
| issued_at | datetime | 지급일 |
| status | enum | 지급대기/지급완료/사용완료 |

---

## 5. 예상 이미지 모형 로직 (1차 구현안)

1. `BMI = 몸무게(kg) / 키(m)²` 로 현재/목표 BMI 산출.
2. BMI 구간을 체형 카테고리로 매핑 (저체중/정상/과체중/비만1/비만2 등).
3. 카테고리별로 사전 제작된 **성별 구분 파라메트릭 실루엣 일러스트 세트**를 매핑.
4. 현재 실루엣 vs 목표 실루엣을 **슬라이더/토글 UI**로 나란히 비교 표시.
5. 신체 정보(키/몸무게)나 목표(목표체중/기간)를 수정하면 즉시 재계산되어 화면이 업데이트.
6. 실제 체중을 주기적으로 입력하면, "현재 vs 목표" 대신 "시작 vs 현재 vs 목표" 3단 비교로 확장.

> 확장 옵션(2차): 사용자가 업로드한 사진에 AI 보정을 적용한 개인화 이미지. 초상권/동의/오남용 방지를 위한
> 별도 약관 동의 및 워터마크("예상 이미지, 실제 결과와 다를 수 있음") 필수.

---

## 6. 3축 가이드 (운동 / 식단 / 보조식품) 구성

다이어트 기간을 3단계(초기/중기/후기)로 분할하여 단계별 가이드 제공:

| 단계 | 운동 가이드 | 식단 가이드 (판매) | 보조식품 가이드 (판매) |
|---|---|---|---|
| 초기 (적응기) | 저강도 유산소, 걷기, 스트레칭 | 저칼로리 균형식, 대체식 | 기초대사 보조제, 비타민 |
| 중기 (본격기) | 근력+유산소 병행, 주 3~4회 | 고단백 식단, 저탄수 식단 | 단백질 보충제, 지방분해 보조제 |
| 후기 (유지기) | 근력 유지, 체형 관리 운동 | 유지식, 리바운드 방지 식단 | 유지용 보조제, 영양 밸런스 제품 |

- 운동은 **가이드(콘텐츠)만 제공** (판매 대상 아님).
- 식단 + 보조식품은 **자사 판매 상품과 직결**되어 플랜 화면에서 바로 장바구니 담기 가능.
- `PlanTemplate.goal_range` 조건(성별, 감량폭, 기간)에 따라 추천 상품이 자동 큐레이션됨.

---

## 7. 화면 설계 (와이어프레임 레벨)

### 7.1 목표 설정 화면
```
[이름] [성별: 남/여] [키: ___cm] [현재 몸무게: ___kg]
[목표 몸무게: ___kg]  [다이어트 기간: ___개월]
                                   [예상 이미지 생성하기]
```

### 7.2 시뮬레이터 화면
```
┌───────────────┬───────────────┐
│  현재 (실루엣) │  N개월 후 (실루엣) │
│   BMI 24.5    │    BMI 21.0    │
└───────────────┴───────────────┘
   [값 수정하기]        [저장하기]  ← 클릭 시 PlanSnapshot 기록
```

### 7.3 플랜/쇼핑 화면
```
[초기] [중기] [후기]  ← 탭
 운동 가이드 카드
 식단 상품 카드 리스트 [장바구니 담기]
 보조식품 상품 카드 리스트 [장바구니 담기]
```

### 7.4 기록 히스토리 화면
```
날짜별 체중 변화 그래프
스냅샷 카드: [2026-01-01: 72kg 이미지] vs [2026-03-01: 68kg 이미지] ... [비교 보기]
```

### 7.5 마이페이지 - 기프트/구매이력
```
구매 이력 테이블 (날짜/상품/금액/상태)
기프트함: [목표 달성 축하 쿠폰 - 사용가능]
```

---

## 8. 기프트(리워드) 로직

- `DietGoal.status`가 `achieved`로 전환되는 조건: 사용자가 입력(또는 연동)한 최신 체중이
  목표일 이내에 목표 체중 이하로 도달.
- 달성 시 자동으로 `Gift` 레코드 생성 → 쿠폰/포인트 지급 → 마이페이지 알림.
- 부분 달성(기간 초과했지만 목표 근접)에 대한 소정의 격려 기프트 규칙은 운영 정책으로 별도 정의 가능.

---

## 9. 기술 스택 제안

- **프론트엔드**: React (또는 Next.js) + Tailwind — 실루엣 시뮬레이터는 SVG/Canvas 기반 컴포넌트
- **백엔드**: Node.js(Express/Nest) 또는 기존 스택 재사용 — REST API
- **DB**: PostgreSQL (정형 데이터 + JSON 컬럼으로 플랜/스냅샷 유연 저장)
- **이미지 처리**: 1차는 정적 일러스트 매핑(서버 로직 불필요), 2차 확장 시 이미지 생성 API 연동
- **결제**: PG 연동 (토스페이먼츠/아임포트 등)
- **인증**: JWT 기반 세션

---

## 10. MVP 범위 제안 (1차 릴리스)

1. 회원가입/로그인
2. 프로필+목표 입력 → BMI 계산 → 실루엣 이미지(현재/목표) 표시
3. 값 수정 시 실시간 재계산 / 저장 시 스냅샷 기록
4. 3단계 플랜 템플릿(하드코딩 or 관리자 등록) 노출
5. 식단/보조식품 상품 목록 + 장바구니 + 주문(더미 결제 or 실 PG 연동)
6. 구매 이력 페이지
7. 목표 달성 시 기프트 자동 지급(쿠폰 발급)

**2차 확장**: 기록 히스토리 비교 그래프 고도화, 사진 기반 개인화 이미지, 추천 알고리즘 고도화,
정기구독(식단 정기배송), 커뮤니티/후기 기능.
